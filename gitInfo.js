/*
    For this section of the assessment you will be putting together a cheat sheet for common git commands.  
    You'll provide the command as well as what it does. 
*/

//////////////////PROBLEM 1////////////////////
/*
    Create a variable called 'gitDefinition'.
    It should be a string containing your best definition of what Git is.
*/

var gitDefinition = '`Git` is a distributed version control system that is used to manage and track changes to files and projects, it's a command line tool that allows developers to keep track of their code and collaborate with other developers.';

//////////////////PROBLEM 2////////////////////
/*
    Create a variable called 'gitHubDefinition'.  
    It should be a string containing your best definition of what GitHub is.
*/

let gitHubDefinition = '`GitHub` is a web-based platform for version control and collaboration that uses Git, allowing developers to store and manage their code, track changes and collaborate with others. It's also a popular platform for open-source projects.';

//////////////////PROBLEM 3////////////////////
/*
    Create a variable called 'gitInitDefinition'.  
    It should be a string containing your best definition of what 'git init' does.
*/

var gitInitDefinition = '`git init` command is used to initialize a new Git repository in the current directory, creating the necessary metadata for version control.';

//////////////////PROBLEM 4////////////////////
/*
    Create a variable called 'gitCloneDefinition'.  
    It should be a string containing your best definition of what 'git clone' does.
*/

let gitCloneDefinition = 'git clone is a command that creates a local copy of a remote repository, allowing you to download the entire repository, including its history and branches, to your local machine and start working on it.';

//////////////////PROBLEM 5////////////////////
/*
    Create a variable called 'gitStatusDefinition'.  
    It should be a string containing your best definition of what 'git status' does.
*/

let gitStatusDefinition = 'git status';

//////////////////PROBLEM 6////////////////////
/*
    Create a variable called 'gitAddDefinition'.  
    It should be a string containing your best definition of what 'git add' does.

    Create another variable called 'gitAddCode'.  
    It should be a string containing the code to add all files.
*/

const gitAddDefinition = '`git add` is a command that stages changes made to files in the working directory, preparing them to be committed to the repository. It can be used to add specific files or all changes in the working directory.';

const gitAddCode = '`git add` . or `git add -A` is a command that stages all changes in the working directory, preparing them to be committed to the repository. ';

//////////////////PROBLEM 7////////////////////
/*
    Create a variable called 'gitCommitDefinition'.  
    It should be a string containing your best definition of what 'git commit' does.

    Create a variable called 'gitCommitCode'.  
    It should be a string containing the code to commit using the message "initial commit".
*/

const gitCommitDefinition = '`git commit` is a command that saves the changes in the local repository.';

const gitCommitCode = '`gitCommitCode: git commit -m "initial commit"` is a command to commit all changes in the local repository with a message "initial commit".';

//////////////////PROBLEM 8////////////////////
/*
    Create a variable called 'gitPushDefinition'.  
    It should be a string containing your best definition of what 'git push' does.
*/

var gitPushDefinition = '`git push` is a command that uploads the local commits to a remote repository.';